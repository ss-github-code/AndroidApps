<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vidsearch.videosearchapp">

    <application
        android:requestLegacyExternalStorage="true"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.ImagesFolderProgressActivity">
        </activity>
        <activity android:name=".ui.VideosFolderProgressActivity">
        </activity>
        <activity
            android:theme="@android:style/Theme.Holo.Light.Dialog.NoActionBar.MinWidth"
            android:name=".ui.FaceNameDialogActivity">
        </activity>
        <activity android:name=".ui.ImagesFoldersActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:required="true" />

</manifest>